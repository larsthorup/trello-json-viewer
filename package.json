{
  "name": "trello-json-viewer",
  "version": "1.0.0",
  "description": "A web application that converts Trello JSON export files into markdown",
  "type": "module",
  "scripts": {
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.3.1"
  }
}
